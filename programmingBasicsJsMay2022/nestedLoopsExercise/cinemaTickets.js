function cinemaTickets(input) {
    let index = 0;
    let command = input[index];
    let moveName = input[index++];
    let freeSpaces = Number(input[index++]);
    let totalTickets = 0;
    let standartTockets = 0;
    let studentTickets = 0;
    let kidsTickets = 0;

    while (command !== 'Finish') {
        let tickets = 0; 
        
        while ()
    }
}